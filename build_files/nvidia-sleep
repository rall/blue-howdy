#!/bin/bash
# NVIDIA suspend/resume hook
# Saves and restores GPU state across S3 sleep cycles

case "$1/$2" in
    pre/suspend|pre/hibernate)
        echo "suspend" > /proc/driver/nvidia/suspend
        ;;
    post/suspend|post/hibernate)
        echo "resume" > /proc/driver/nvidia/suspend
        ;;
esac
